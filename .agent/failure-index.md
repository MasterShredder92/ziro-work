# Failure Index

| Date | Domain | Method That Failed | Why It Failed | Use Instead |
|---|---|---|---|---|
| 2026-05-19 | DB Migration | Writing PL/pgSQL with assumed column types (text vs date for students.start_date, first_lesson_date) | Column types not confirmed before writing SQL — caused type assignment errors in enroll_student RPC | Run `SELECT column_name, data_type FROM information_schema.columns WHERE table_name='students'` before writing UPDATE statements with type casts |
| 2026-05-19 | DB Migration | Using jsonb_build_object()::text for activity_log.details without confirming column type | Column may be jsonb (no cast needed) or text (::text required) — must verify first | Query `information_schema.columns WHERE table_name='activity_log' AND column_name='details'` before writing INSERT |
| 2026-05-19 | DB Migration | ON CONFLICT clause missing tenant_id for recurring_lessons | Assumed unique constraint was on (student_id, teacher_id, location_id, day_of_week, start_time); actual index includes tenant_id prefix | Always query `pg_indexes WHERE tablename=<table>` before writing ON CONFLICT — do not assume which columns are in the unique constraint |
| 2026-05-19 | RLS Policy | Used `get_my_tenant_id() RETURNS uuid` function — failed with `operator does not exist: text = uuid` | Financial table `tenant_id` columns are mixed types (15 text, 7 uuid); function returned uuid, incompatible with text tenant_id columns | Skip helper function; inline `current_setting('app.tenant_id'::text, true)` for text columns, append `::uuid` cast for uuid columns. Always query `information_schema.columns WHERE column_name='tenant_id'` before writing policies |
| 2026-05-19 | RLS Policy | Specified JWT claim path `::jsonb->>'tenant_id'` (top-level) — tenant_id is actually nested | In Supabase, `tenant_id` is under `app_metadata`, not top-level JWT claims. Correct path: `::jsonb->'app_metadata'->>'tenant_id'` | Verify JWT structure via `SELECT raw_app_meta_data FROM auth.users LIMIT 1` before using `current_setting('request.jwt.claims')` in policies |
| 2026-05-19 | RLS Policy | Assumed `current_setting('app.tenant_id')` approach matches JWT claims approach | Codebase uses session variable `app.tenant_id`, not JWT claims. Existing policies on `lessons`, `agreements`, `portalsessions` confirm session variable is the canonical pattern | Check existing `pg_policies` on any working table before designing a new RLS pattern; match the codebase standard |
