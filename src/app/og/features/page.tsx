import { SocialPreviewCard } from "@/components/brand/SocialPreviewCard";

export default function OgFeaturesPage() {
  return (
    <SocialPreviewCard
      title="Features built for operators"
      subtitle="Studio map, lifecycle spine, billing signals, and command surfaces."
    />
  );
}
