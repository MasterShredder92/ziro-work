import { SocialPreviewCard } from "@/components/brand/SocialPreviewCard";

export default function OgPricingPage() {
  return (
    <SocialPreviewCard
      title="Pricing that stays honest"
      subtitle="Launch, Scale, and Command—tiers tuned for roster reality."
    />
  );
}
