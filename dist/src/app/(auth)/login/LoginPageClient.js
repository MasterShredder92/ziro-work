"use client";
import { jsx as _jsx } from "react/jsx-runtime";
import { LoginForm } from "./LoginForm";
export function LoginPageClient({ accent, nextHref, }) {
    return _jsx(LoginForm, { accent: accent, nextHref: nextHref });
}
