import { jsx as _jsx } from "react/jsx-runtime";
import { SocialPreviewCard } from "@/components/brand/SocialPreviewCard";
export default function OgFeaturesPage() {
    return (_jsx(SocialPreviewCard, { title: "Features built for operators", subtitle: "Studio map, lifecycle spine, billing signals, and command surfaces." }));
}
