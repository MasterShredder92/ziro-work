name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  lint-and-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Service-role guard
        run: |
          violations=$(grep -rl "getServiceClient" src/app/api/ | while read -r f; do
            grep -q "assertServiceRoleAllowed" "$f" || echo "$f"
          done)
          if [ -n "$violations" ]; then
            echo "ERROR: These files use getServiceClient() without assertServiceRoleAllowed():"
            echo "$violations"
            exit 1
          fi
          echo "Service-role guard check passed."

      - name: Lint
        run: npm run lint

      - name: API smoke tests
        run: npm run test:smoke
